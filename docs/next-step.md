# Следующий шаг после v0.6.24

1. Прогнать обновлённый GitHub Actions workflow и получить либо зелёную сборку, либо уже настоящий `vite`/bundle error с подробным логом.
2. Если build пройдёт, вернуться к поэлементной зачистке `TasksPage.vue`.
3. Отдельным этапом продолжить проектирование серверной проверки сертификатов Провайдеров именно для Ubuntu-хоста, без смешивания с роутерным проектом.
