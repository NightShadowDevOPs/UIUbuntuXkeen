# Следующий шаг после v0.6.26

1. Запустить workflow из релиза `v0.6.26`.
2. Получить первую реальную ошибку `vite build` из лога `Build UI`.
3. Делать следующий hotfix только по фактической ошибке bundling/rollup/import resolution.
4. После стабилизации CI вернуться к cleanup `TasksPage` и серверной архитектуре проверки SSL Провайдеров.
