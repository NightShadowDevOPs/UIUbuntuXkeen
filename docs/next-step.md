# Следующий шаг после v0.6.42

1. Провести безопасный аудит текущего backend-контура UIUbuntuXkeen без выдуманных сервисов и без смешивания с роутерной веткой.
2. Отдельно определить, где внутри существующего проекта должны храниться данные для **Хостов 3x-ui** и **Пользователей**.
3. После этого реализовать backend-endpoints для:
   - списка 3x-ui host panel URLs;
   - статуса сертификата, даты окончания и времени последней проверки;
   - users inventory (`IP / MAC / hostname / proxyAccess`);
   - режима доступа к proxy.
4. Для каждого следующего шага делать smoke-check recovery/update-механизма через `mihomo`.
