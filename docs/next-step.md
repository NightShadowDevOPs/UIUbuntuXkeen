# Следующий шаг после v0.6.21

## v0.6.21 — после второго build hotfix

1. Добить cleanup `TasksPage.vue` по блокам и вынести из него legacy users-db/compatibility элементы, которые не относятся к штатному Ubuntu/server потоку.
2. Подготовить отдельный честный server-side этап для проверки сертификатов Провайдеров на Ubuntu-хосте.
3. Не смешивать этот проект с роутерным runtime и не возвращать fake provider SSL flow в браузер.


## v0.6.21 — после build hotfix

Сборка должна снова проходить без падения на TS/nullability.

## Следующий продуктовый шаг — Tasks teardown by blocks

Первый приоритет — `TasksPage.vue`.

Нужно разобрать блоки страницы по четырём корзинам:
- оставить;
- временно скрыть;
- перенести в будущий серверный backend;
- удалить как router legacy.

Отдельно для блока Провайдеров нужно зафиксировать:
- какие поля действительно нужны пользователю уже сейчас;
- какие кнопки нельзя показывать как рабочие без server-side контура;
- какой минимальный UI останется до появления настоящей серверной проверки SSL сертификатов.

## После Tasks

### v0.6.21
- пересобрать `SubscriptionsPage.vue` под канонический server publish path;
- убрать роутерную терминологию и зависимости как модель продукта.

### v0.6.21
- начать зачистку `UsersPage.vue` и `PoliciesPage.vue`;
- решить, что из этого реально нужно Ubuntu/server-проекту.


## После v0.6.21

1. Спроектировать и поднять отдельный Ubuntu service на хосте.
2. Перенести хранение URL подписок с browser-only слоя в server-side state.
3. Вернуть в `Задачи` кнопку ручного запуска проверки только после появления реального backend execution path.
