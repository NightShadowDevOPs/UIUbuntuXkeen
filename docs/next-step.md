# Следующий шаг после v0.6.45

1. Проверить на живом хосте, что capability-store больше не пустой и что экраны `Хосты 3x-ui` и `Пользователи` реально сохраняют/читают данные через текущий `api.sh` contour.
2. Добить server-side применение `proxyAccess` так, чтобы политика могла выполняться не только фронтом, но и отдельной server-side командой.
3. При необходимости добавить в `api.sh` отдельные узкие команды под `3x-ui Hosts` и `Users`, но без нового отдельного сервиса.
