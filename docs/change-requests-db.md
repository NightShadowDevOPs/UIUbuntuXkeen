# База запросов на изменение

Актуально для релиза: **v0.6.41**

## 2026-03-31 — rollback phantom service
Запрос пользователя:
- убрать фантомный `service` из ниоткуда;
- не тащить отдельную сущность на сервер без существующей базы и контекста;
- продолжать проект только через текущий backend-контур;
- сохранить отдельные разделы **Хосты 3x-ui** и **Пользователи**;
- хранить каждый запрос и каждый шаг в документации проекта.

## Решение в v0.6.41
- отдельный `ubuntu-service/` удалён из релиза;
- текущие документы переведены в честное состояние;
- сохранён UI-контур для 3x-ui host SSL checks и users inventory без новой отдельной server-side сущности.

- 2026-03-31: reported blank `/ui/`, direct section links still work, in-app update unavailable; required urgent hotfix.
