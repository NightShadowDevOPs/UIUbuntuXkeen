# v0.6.37 — separate 3x-ui hosts workspace and visible Users tab

Что исправлено:
- адреса панелей 3x-ui вынесены в отдельный раздел меню **3x-ui Hosts**;
- страница работает только с `providerPanelUrls` из общей БД;
- опрос сертификатов делает backend router-agent (`api.sh` / `router-agent/install.sh`) с автообновлением каждые 4 часа и ручным запуском;
- в UI выводятся статус сертификата, дата окончания и время последней проверки;
- вкладка **Пользователи** добавлена в левое меню.
