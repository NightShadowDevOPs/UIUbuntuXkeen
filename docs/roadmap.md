# Roadmap

Текущая версия линии: **v0.6.27**

Ближайший технический шаг:
- довести GitHub Actions до состояния, когда workflow показывает настоящую причину build-падения, а не ломается на bootstrap шаге.

Следующий продуктовый шаг после стабилизации сборки:
- продолжить cleanup `TasksPage.vue` и дальнейшее разделение Ubuntu host UI от роутерного legacy.

Отдельный будущий блок:
- реализовать server-side проверку SSL сертификатов Провайдеров как функцию отдельного Ubuntu service на хосте проекта.
