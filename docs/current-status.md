# Текущий статус — v0.6.25

- Текущая линия: **v0.6.25**
- `v0.6.17` остаётся ошибочным архитектурным ответвлением и не считается правильной базой проекта.
- `v0.6.19` честно отключил фальшивый provider SSL flow через legacy path.
- `v0.6.20`–`v0.6.22` закрывали TypeScript/CI build blockers.
- `v0.6.23` добавил подробный лог `vite build`, но сам workflow всё ещё мог падать раньше сборки из-за отсутствующего `pnpm`.
- `v0.6.25` исправляет bootstrap workflow: `pnpm` теперь ставится явно перед install/build.

## Что это значит

Теперь GitHub Actions должен либо реально пройти сборку, либо наконец показать уже содержательную ошибку `vite build`, а не умереть на пустом месте с `Unable to locate executable file: pnpm`.

- `v0.6.25` усиливает раннюю диагностику GitHub Actions и убирает вторичное падение шага вывода build-лога.
