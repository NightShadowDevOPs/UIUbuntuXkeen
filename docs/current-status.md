Current prepared release: v0.6.42. Stabilizing cleanup after the recovered v0.6.41 baseline. The distribution no longer carries `router-agent/` or root helper leftovers, and the actual recovery path is now fixed in docs: stop `mihomo`, remove the UI directory, start `mihomo` again, let Mihomo redownload the UI from the repository.
