# Текущий статус — v0.6.26

- Текущая линия: **v0.6.26**
- `v0.6.17` остаётся ошибочным архитектурным ответвлением и не считается правильной базой проекта.
- `v0.6.19` честно отключил фальшивый provider SSL flow через legacy path.
- `v0.6.20`–`v0.6.22` закрывали TypeScript/CI build blockers.
- `v0.6.23`–`v0.6.25` усиливали GitHub Actions диагностику, но workflow всё ещё спотыкался о bootstrap/logging слой раньше, чем показывал реальную ошибку Vite.
- `v0.6.26` убирает `pnpm/action-setup` и ставит `pnpm` через `npm install -g`, а также печатает `build-ui.log` прямо в падающем шаге `Build UI`.

## Что это значит

Теперь GitHub Actions должен либо реально пройти сборку, либо наконец показать уже содержательную ошибку `vite build`, а не падать на bootstrap-слое workflow.
