# UIUbuntuXkeen

- Текущая версия линии: **v0.6.42**
- Текущий шаг: **stabilize / cleanup after v0.6.41 recovery**
- Каноничный recovery-механизм подтверждён пользователем: **остановить `mihomo` → удалить папку UI → запустить `mihomo` снова**. После старта `mihomo` сам заново скачивает UI из репозитория.
- Каталог **`router-agent/` удалён из дистрибутива** как чужой хвост роутерной ветки.
- Каноничный путь дальше: встраивать хранение, SSL-проверки 3x-ui хостов и таблицу пользователей **в существующий backend проекта**, а не тащить новые сущности на сервер.

## Что в v0.6.42
- удалён каталог `router-agent/` из дистрибутива;
- удалён root-мусор `_api*.sh`, `_backup_new.sh`, `extracted_api*.sh`;
- зафиксирован реальный recovery/update-механизм через `mihomo`;
- обновлены README / CHANGELOG / transfer docs / document DB;
- релиз не добавляет нового backend-а и не меняет текущий runtime-контур сервера.

## Дальше
1. Сделать безопасный аудит текущего backend-контура UIUbuntuXkeen без выдуманных сервисов.
2. Отдельно спроектировать хранение данных для **Хостов 3x-ui** и **Пользователей** внутри существующего проекта.
3. После этого уже внедрять backend-endpoints под SSL checks, users inventory и proxy access policy.
