# UIUbuntuXkeen

- Текущая версия линии: **v0.6.40**
- Текущий шаг: **cleanup phantom service and return to the existing backend contour**
- Отдельный `ubuntu-service/` из v0.6.39 **удалён из релиза**.
- Каноничный путь дальше: встраивать хранение, SSL-проверки 3x-ui хостов и таблицу пользователей **в существующий backend проекта**, а не тащить новый отдельный service на сервер.

## Что в v0.6.40
- удалён каталог `ubuntu-service/` из дистрибутива;
- убраны инструкции по развёртыванию отдельного service;
- UI отвязан от обязательного режима `ubuntu-service` и ориентируется на возможности текущего backend-а;
- сохранены отдельные разделы **Хосты 3x-ui** и **Пользователи**;
- документация обновлена: фантомный service признан ошибочной веткой и откачен.

## Дальше
1. Встроить endpoints для 3x-ui host SSL checks в текущий backend-контур проекта.
2. Встроить хранение таблицы пользователей `IP / MAC / hostname / proxyAccess` в текущую БД backend-а.
3. После этого включить реальную автопроверку сертификатов каждые 4 часа и ручной запуск из UI.
